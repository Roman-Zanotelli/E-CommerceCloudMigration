apiVersion: v1
kind: Service
metadata:
  name: {{ include "easy-core.fullname" . }}
  labels:
    {{- include "easy-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "easy-core.selectorLabels" . | nindent 4 }}
