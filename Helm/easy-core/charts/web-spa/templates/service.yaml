apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-spa.fullname" . }}
  labels:
    {{- include "web-spa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-spa.selectorLabels" . | nindent 4 }}
