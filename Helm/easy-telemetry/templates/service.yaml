apiVersion: v1
kind: Service
metadata:
  name: {{ include "easy-telemetry.fullname" . }}
  labels:
    {{- include "easy-telemetry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "easy-telemetry.selectorLabels" . | nindent 4 }}
