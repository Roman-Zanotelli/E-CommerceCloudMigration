apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "easy-telemetry.fullname" . }}-test-connection"
  labels:
    {{- include "easy-telemetry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "easy-telemetry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
